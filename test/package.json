{
  "name": "test-merlmovie-sdk",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "harryodinson",
  "main": "./dist/index.js",
  "scripts": {
    "test": "cd ../ && npm run build && cd test && npm install ../ && tsc --build --verbose && node ./dist/index.js",
    "dev": "cd ../ && npm run build && cd test && npm install ../ && tsc --build --verbose && nodemon ./src/index.ts"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@types/express": "^5.0.1",
    "@types/ws": "^8.18.1",
    "express": "^5.1.0",
    "merlmovie-sdk": "file:..",
    "puppeteer": "^24.17.0",
    "ws": "^8.18.1"
  }
}
